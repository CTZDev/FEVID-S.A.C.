---
export interface Props {
  href?: string;
  target?: string;
  src: string;
  alt: string;
  classPosition: string;
  classImg: string;
  onclick?: string;
}

const { href, target, src, alt, classPosition, classImg, onclick } = Astro.props;
---

<a
  {...Astro.props}
  href={href}
  target={target}
  onclick={onclick}
  class={`cursor-pointer fixed rounded-full transform hover:scale-105 transition-transform  ${classPosition} z-10`}>
  <img src={src} alt={alt} class={classImg} />
</a>
